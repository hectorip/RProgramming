# Si al declarar un vector, no todos los elementos
# son del mismo tipo, R intentará hacerlos del mismo, lo que se llama "coerción"
# pudiendo dar resultados inesperados.

un_vector_mas <- c(1, "1.89", 12) # todo lo convertirá a tipo character